
запуск
удаляем last_date от прошлых скачек

в setting.cfg задает url такой, который есть на первой странице



в соответствии с шаблоном регулярных выражений 
новый curl с www и вопросом на конце:
http://www.ufcstats.com/event-details/542db012217ecb83?



м.б. проблемы
startListProductsParser(self, messages_queue, product_last_datetime):
порядок поменял если есть вызовы без параметров startListProductsParser

Порядок работы с прогой
       
       
       Работа 
       С нуля – скачиваем до исключений, меняем ip и продолжаем
       	Если не закончили то останется файл page_params, который покажет последнюю запись (скачанной реквизиты, по ним находим в браузере и корректируем файл)
       Если закончили то сохранится last_date – с текущей датой (может чуть сдвинуть надо будет, если за время скачки новые записи появились)
       Не с нуля 
       		Есть last_date – результат пойдет во flats_add
       		Нет last_date – если утеряли (настраиваем поле с___)
       				Если нет, то случай а)
       
       Перед использование программы в корне создать папку ark – сюда будут копироваться предыдущие файлы flats

